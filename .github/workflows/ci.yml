name: ロジックテスト

on:
  push:
    branches:
      - main
      - "feature/*"
  pull_request:
  workflow_dispatch:

jobs:
  test:
    runs-on: macos-14
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Run tests
        run: |
          swift test
